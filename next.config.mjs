/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'cdn.shopify.com',
        'images.ctfassets.net'
      ],
      },
};

export default nextConfig;
